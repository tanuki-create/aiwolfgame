{
  "name": "@aiwolf/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "bun test",
    "init-db": "bun run src/init-db.ts",
    "simulate": "bun run src/simulation/runner.ts"
  },
  "dependencies": {
    "@aiwolf/shared": "workspace:*",
    "@aiwolf/fsm": "workspace:*",
    "@aiwolf/llm": "workspace:*",
    "@aiwolf/db": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}

